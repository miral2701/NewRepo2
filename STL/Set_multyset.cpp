#include<iostream>
#include<set>
#include<map>

using namespace std;

typedef pair<int, string> mypair;//typrdef существующий тип псевдоним 
int main() {
	/*set<int>arr;
	arr.insert({ 4,5,2,3,9 });
	for (auto ptr = arr.begin(); ptr != arr.end(); ptr++) {
		cout << *ptr;
	}

	map<int, string> group;
	group.insert(mypair(2, "Mira"));
	group.insert(mypair(3, ",scsca"));
	group.insert(mypair(5, "Ivan"));

	for (auto ptr = group.begin(); ptr != group.end(); ptr++) {
		cout << ptr->first;
		cout << ptr->second;

	}*/

	map<string, string> dict;
}